#include <iostream>
#include "AutoPlayer.h"
#include "BoardGenerator.h"

int main() {
    std::cout << "Benchmark stub rodando. Core linkado com sucesso.\n";

    // Exemplo de uso futuro:
    // ms::Board board = ms::BoardGenerator::generate(8, 8, 10, 3, 3);
    // ms::GameResult r = ms::AutoPlayer::playGame(board, 1000);

    return 0;
}
